<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- android:icon="@drawable/app_icon" -->
    <!-- android:roundIcon="@drawable/app_icon" -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/garuda"
        android:label="@string/app_name"
        android:roundIcon="@drawable/garuda"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventAdmin"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.EventAdmin.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.EventAdmin.NoActionBar" />

        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:label="Dashboard" />

        <activity
            android:name=".ListActivity"
            android:exported="false"
            android:label="Registrations List"
            android:parentActivityName=".DashboardActivity" />

        <activity
            android:name=".MilestoneCelebrationActivity"
            android:exported="false"
            android:theme="@style/Theme.EventAdmin.NoActionBar"
            android:screenOrientation="portrait" />

        <!-- Widget Receiver -->
        <receiver
            android:name=".MoneyWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

    </application>

</manifest>
